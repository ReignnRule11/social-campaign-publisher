repos:
  - repo: local
    hooks:
      - id: detect-webhook-urls
        name: Detect webhook URLs
        entry: python scripts/check_secrets.py --staged
        language: system
        stages: [commit]
